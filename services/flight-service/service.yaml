apiVersion: v1
kind: Service
metadata:
  name: svc-flight
  namespace: airline-flight
  labels:
    app: flight
spec:
  selector:
    app: flight
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP