apiVersion: v1
kind: Service
metadata:
  name: svc-payment
  namespace: airline-payment
spec:
  selector:
    app: airline-payment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
